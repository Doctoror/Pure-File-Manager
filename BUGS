- sometimes while copy or paste you can get a fc: reported, but can't reproduce yet. Maybe already fixed?
- could not delete directory from busybox non-root on emulator
- history is cleared on appearance change
- file picker not working
- Sometimes can't focus to AbsListView
- BreadCrumbTextView is vulnerable: file must not contain " > " or it will misbehave